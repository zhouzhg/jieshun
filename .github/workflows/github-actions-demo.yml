name: learn-github-actions
on: [pull_request]
jobs:
  check-bats-version:
    runs-on: ubuntu-latest
    steps:
      - name: copy file
        shell: bash
        run: | 
          # echo The username is ${{ inputs.username }}
          echo "======${{ github.workspace }}======"
          echo The PR was merged
          echo `pwd`
          touch a.txt && echo "11111111112222222" >> a.txt
          ls -l
          echo "=================查看输入信息================="
          cat a.txt
